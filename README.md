# @berish/mvc-stateful-plugin

Stateful plugin for @berish/mvc-core

## Installation

```
$ npm install @berish/mvc-stateful-plugin --save
```

or

```
$ yarn add @berish/mvc-stateful-plugin
```

**Supports typescript**
